{
  "manifest_version": "0.3",
  "name": "weavatrix-js",
  "display_name": "Weavatrix JS",
  "version": "0.3.15",
  "description": "Fully local repository intelligence MCP: always-fresh graph, LSP-backed impact, production-first Health, dead code, endpoints, duplicates, history and architecture safeguards.",
  "long_description": "Weavatrix builds dependency graphs of local repositories with bundled tree-sitter WASM and a bounded TypeScript/JavaScript language-server provider. It serves 34 network-free MCP tools for graph queries, source context, proof-carrying changes, Health, dependency analysis, clone review, history, architecture rules and cross-repository static contracts. The offline profile enables all local tools; pinned removes repository retargeting and cross-repository access.",
  "author": {"name": "Sergii Ziborov", "url": "https://weavatrix.com"},
  "homepage": "https://weavatrix.com",
  "documentation": "https://github.com/sergii-ziborov/weavatrix-js#readme",
  "privacy_policies": ["https://weavatrix.com/privacy"],
  "license": "MIT",
  "keywords": ["code-graph", "impact-analysis", "blast-radius", "static-analysis", "dead-code", "mcp", "codex", "openai-codex", "coding-agents"],
  "icon": "icon.png",
  "tools_generated": true,
  "server": {
    "type": "node",
    "entry_point": "bin/weavatrix-mcp.mjs",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/bin/weavatrix-mcp.mjs", "${user_config.repo_root}", "${user_config.capabilities}"],
      "env": {
        "WEAVATRIX_PRECISION": "${user_config.precision}",
        "WEAVATRIX_PRECISION_PREWARM": "${user_config.precision_prewarm}",
        "WEAVATRIX_ALLOW_TEST_RUNS": "${user_config.allow_test_runs}"
      }
    }
  },
  "user_config": {
    "repo_root": {
      "type": "directory",
      "title": "Repository to analyze",
      "description": "The initial local Git repository. The offline profile can switch only through an explicit local open_repo call; select pinned to make this boundary immutable.",
      "required": true
    },
    "capabilities": {
      "type": "string",
      "title": "Local capability profile or capability groups",
      "description": "Use offline (default) or pinned. Advanced: exact comma-separated graph,search,source,health,build,retarget,crossrepo. Online/Cloud/Enterprise tools belong to the separate weavatrix-online package.",
      "default": "offline",
      "required": true
    },
    "precision": {
      "type": "string",
      "title": "TypeScript/JavaScript semantic precision",
      "description": "Use lsp (default) for the bundled bounded language-server overlay, or off for parser-only graphs.",
      "default": "lsp",
      "required": true
    },
    "precision_prewarm": {
      "type": "string",
      "title": "Semantic prewarm budget",
      "description": "Use bounded (default) for fast startup, or full for an explicit high-budget pass over every eligible TypeScript/JavaScript target within hard time, source and memory ceilings.",
      "default": "bounded",
      "required": true
    },
    "allow_test_runs": {
      "type": "string",
      "title": "Allow verified_change test execution",
      "description": "Set to 1 only when verified_change may execute explicitly requested test/check/verify package.json scripts. Default 0 keeps the workflow read-only.",
      "default": "0",
      "required": true
    }
  },
  "compatibility": {"platforms": ["win32", "darwin", "linux"], "runtimes": {"node": ">=18"}}
}
